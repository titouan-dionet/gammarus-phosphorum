% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population_dynamics.R
\name{elasticity_matrix}
\alias{elasticity_matrix}
\title{Elasticity matrix determination}
\usage{
elasticity_matrix(Leslie_matrix)
}
\arguments{
\item{Leslie_matrix}{Matrix or data frame. A Leslie matrix.}
}
\value{
A numeric matrix representing the elasticity matrix of the given Leslie matrix.
}
\description{
This function calculates the elasticity matrix of a given Leslie matrix.
}
\details{
The elasticity matrix quantifies how sensitive the asymptotic growth rate (lambda) is to
proportional changes in each element of the Leslie matrix. The calculation involves:
\enumerate{
\item Finding the dominant eigenvalue (lambda) and right eigenvector (w)
\item Finding the left eigenvector (v) by transposing the Leslie matrix
\item Calculating the sensitivity matrix as (v ⊗ w) / <v,w>
\item Converting to elasticity: (a_ij/λ) * sensitivity_ij
}

Where a_ij is the element in row i, column j of the Leslie matrix.
}
\examples{
\dontrun{
# Calculate elasticity matrix
elasti_mat <- elasticity_matrix(Leslie_matrix = leslie_mat)

# Calculate elasticities by category
survival_elasticity <- sum(diag(as.matrix(elasti_mat)))
growth_elasticity <- sum(elasti_mat[lower.tri(elasti_mat)])
fecondity_elasticity <- sum(elasti_mat[upper.tri(elasti_mat)])
}

}
