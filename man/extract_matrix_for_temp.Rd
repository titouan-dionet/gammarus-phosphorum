% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population_dynamics.R
\name{extract_matrix_for_temp}
\alias{extract_matrix_for_temp}
\title{Extract Sub-Matrix for a Specific Temperature}
\usage{
extract_matrix_for_temp(matrix_data, temps, current_temp)
}
\arguments{
\item{matrix_data}{Matrix or data frame. The concatenated matrix containing data for multiple temperatures.}

\item{temps}{Numeric vector. Vector of temperature values corresponding to the order of concatenation in the matrix.}

\item{current_temp}{Numeric. The specific temperature value for which to extract the sub-matrix.}
}
\value{
A matrix containing only the rows corresponding to the specified temperature.
}
\description{
This function extracts a subset of rows from a matrix that corresponds to a specific
temperature value when matrices for multiple temperatures have been concatenated.
}
\details{
When using targets with dynamic branching, matrices for different temperature values are
often concatenated into a single matrix. This function extracts the subset of rows
that correspond to a specific temperature value.

The function assumes that:
\enumerate{
\item The matrix rows are organized by temperature
\item Each temperature has the same number of rows
\item Temperatures are in the same order as provided in the 'temps' parameter
}
}
\examples{
\dontrun{
# Assuming matrices have been concatenated for temps = c(8, 12, 16)
# Extract matrix for temperature = 12
sub_matrix <- extract_matrix_for_temp(
  matrix_data = concatenated_matrix,
  temps = c(8, 12, 16),
  current_temp = 12
)
}

}
